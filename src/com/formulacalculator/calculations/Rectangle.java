package com.formulacalculator.calculations;

public class Rectangle {
    public float calculateRectanglePerimeter(float side1, float side2) {
        return (side1 + side1) + (side2 + side2);
    }

    public float calculateRectangleArea(float side1, float side2) {
        return side1 * side2;
    }
}
